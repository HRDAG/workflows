name: Check makefiles

on:
  push:
    branches: test-workflow

jobs:
    test-check-workflow:
      name: Test check makefile workflow
      runs-on: ubuntu-latest
      steps:
        - uses: HRDAG/workflows.git/.github/workflows/makefile-check.yaml@test-workflow
