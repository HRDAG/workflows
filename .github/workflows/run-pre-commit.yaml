name: Run pre-commit

on:
  workflow_call:
    inputs: {}
    secrets: {}

jobs:
    run-pre-commit:
      name: Run pre-commit
      runs-on: ubuntu-latest
      steps:
        - run: echo Hello workflow
